name: Publish to pub.dev

on:
  push:
    tags:
      - 'scroll_spy-v*'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  publish:
    permissions:
      id-token: write
    uses: dart-lang/setup-dart/.github/workflows/publish.yml@v1
