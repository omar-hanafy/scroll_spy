name: scroll_spy
description: Compute focused items and a stable primary item in a scrollable viewport (feed/autoplay/analytics).
version: 0.2.6
repository: https://github.com/omar-hanafy/scroll_spy
issue_tracker: https://github.com/omar-hanafy/scroll_spy/issues
screenshots:
  - description: Scroll Spy icon
    path: screenshots/scroll_spy.png

topics:
  - flutter
  - scroll
  - viewport
  - feed
  - autoplay

environment:
  sdk: ^3.6.0
  flutter: ">=3.27.0"

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
